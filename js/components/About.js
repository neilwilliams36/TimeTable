/** @jsx React.DOM */
var React = require('react');

module.exports= React.createClass({
    render: function(){
        return (<h1>This app is being written entirely in Javascript using React JS and Flux</h1>);
    }
});

